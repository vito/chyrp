{% extends "layout.twig" %}

{% block title %}${ "Site Configuration" | translate }{% endblock %}

{% block content %}
			<h1>${ "Site Configuration" | translate }</h1>
			<form id="general_settings" class="split" action="{% admin "settings" %}" method="post" accept-charset="utf-8" enctype="multipart/form-data">
				<fieldset>
					<p>
						<label for="name">${ "Site Name" | translate }</label>
						<input class="text" type="text" name="name" value="${ site.name | escape }" id="name" />
					</p>
					<p>
						<label for="description">${ "Description" | translate }</label>
						<textarea name="description" rows="2" cols="40">${ site.description | escape }</textarea>
					</p>
					<p>
						<label for="url">${ "Chyrp URL" | translate }</label>
						<input class="text" type="text" name="file_url" value="${ site.file_url | escape }" id="url" />
					</p>
					<p>
						<label for="url">${ "Alternate URL" | translate }</label>
						<input class="text" type="text" name="url" value="${ site.url | escape }" id="url" />
						<small>
							${ "Enter an alternate address here if you want your homepage URL to be different from the URL where Chyrp is normally available." | translate }
						</small>
					</p>
					<p>
						<label for="email">${ "Contact E-Mail Address" | translate }</label>
						<input class="text" type="text" name="email" value="${ site.email | escape }" id="email" />
					</p>
					<p>
						<label for="time_offset">${ "Time Offset" | translate }</label>
						<input class="text" type="text" name="time_offset" value="${ site.time_offset / 3600 }" id="time_offset" />
						<small>
							${ "(server time: %s)" | translate | format(now_server | date) }
						</small>
					</p>
					<p>
						<label for="locale">${ "Language" | translate }</label>
						<select name="locale" id="locale">
							{% for locale in locales %}
							<option value="$locale.code"${ locale.code | option_selected(site.locale) }>$locale.name</option>
							{% endfor %}
						</select>
					</p>

					<p class="buttons">
						<button type="submit" class="yay"><img src="images/icons/success.png" alt="success" />${ "Update" | translate }</button>
					</p>

					<input type="hidden" name="page" value="general" id="id" />
					<input type="hidden" name="hash" value="$site.secure_hashkey" id="hash" />
				</fieldset>
			</form>
{% endblock %}
