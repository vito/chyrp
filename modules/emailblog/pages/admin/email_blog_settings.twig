{% extends "layout.twig" %}

{% block title %}${ "Email to Blog Settings" | translate("emailblog") }{% endblock %}

{% block content %}
            <form id="comment_settings" class="split" action="{% admin "email_blog_settings" %}" method="post">
                <fieldset>
                    <p>
                        <label for="email">${ "Email Address" | translate("emailblog") }</label>
                        <input class="text" type="text" name="email" value="${ site.email_blog_address | escape }" id="email" />
                        <small>${ "Currently only supports gmail accounts. Note: please set up a specialized mailbox for this and not a normal inbox, otherwise all messages that you recieve will be added as posts." | translate("emailblog") }</small>
                    </p>
                     <p>
                        <label for="pass">${ "Email Password" | translate("emailblog") }</label>
                        <input class="text" type="text" name="pass" value="${ site.email_blog_pass | escape }" id="email" />
                        <small>${ "Password to the above email address" | translate("emailblog") }</small>
                    </p>
                    <p>
                        <label for="minutes">${ "Check for new Messages Every __ Minutes" | translate("emailblog") }</label>
                        <input class="text" type="text" name="minutes" value="${ site.email_blog_minutes | escape }" id="email" />
                        <small>${ "Decreasing the time between will result in faster posting but a slower site." | translate("emailblog") }</small>
                    </p>

                    <p class="buttons">
                        <button type="submit" class="yay"><img src="$theme_url/images/icons/success.png" alt="success" />${ "Update" | translate }</button>
                    </p>

                    <input type="hidden" name="hash" value="$site.secure_hashkey" id="hash" />
                </fieldset>
            </form>
{% endblock %}
