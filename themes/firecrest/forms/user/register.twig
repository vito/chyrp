{% extends "layouts/default.twig" %}

{% block content %}
    <h2>${ "Register" | translate }</h2>
    <form action="{% url "register" %}" method="post">
        <p>
            <label for="login">${ "Username" | translate }</label>
            <input type="text" name="login" autofocus="autofocus" value="${ POST.login | escape }" id="login" />
        </p>
        <p>
            <label for="password1">${ "Password" | translate }</label>
            <input type="password" name="password1" value="" id="password1" />
        </p>
        <p>
            <label for="password2">${ "Password " | translate }<span class="sub">${ "(again)" | translate }</span></label>
            <input type="password" name="password2" value="" id="password2" />
        </p>
        <p>
            <label for="email">${ "E-Mail" | translate }</label>
            <input type="text" name="email" value="${ POST.email | escape }" id="email" />
        </p>
        {% if site.enable_recaptcha and enabled_modules.recaptcha %}
            $captcha
        {% endif %}
        ${ trigger.call("user_register") }

        <p><button type="submit">${ "Register" | translate }</button></p>
    </form>
{% endblock %}
