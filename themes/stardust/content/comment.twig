		<li id="comment_$comment.id">
{% if comment.status == "denied" %}
			<em>${ "Your comment is awaiting moderation." | translate }</em>
{% endif %}
			<blockquote>
				$comment.body
			</blockquote>
			<cite>
				${ "<strong>%s</strong> on %s" | translate | format(comment.author_link, comment.created_at | strftime("%B %d, %Y, at %I:%M %p")) }
				${ comment.delete_link("Delete" | translate, "") }
				${ comment.edit_link("Edit" | translate, "") }
			</cite>
		</li>
