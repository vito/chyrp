tags_top:

admin_manage_nav:

rss_comment:
	$comment->id
comments_rss_item:
	$comment->id
admin_manage_comments_column_header:

manage_comments:

admin_manage_comments_column:
	$comment->id
manage_comments:

show_comment:
	$comment
update_comment:
	$this
delete_comment:
	new self($comment_id)
top:

bottom:

pingback:
	array($id, $linked_to, $linked_from, $title, $excerpt)
trackback_receive:

add_user:
	$id
update_user:
	array($this->id, $login, $password, $full_name, $email, $website, $group_id)
add_post:
	array($id, $options)
update_post:
	array($this, $options)
filter_post:
	$this
add_page:
	$id
update_page:
	$this->id
add_group:
	array($id, $name, $permissions)
update_group:
	array($this, $name, $permissions)
javascript_domready:

ajax_post_edit_form_javascript:

javascript:

edit_post_options:
	$post_id
feed_item:
	$post->id
change_settings:
	$_GET['sub']
runtime:

not_found:

below_post:

ajax:

import_wordpress_post:
	array($the_post, $id)
import_wordpress_page:
	array($the_post, $id)
import_tumble:
	array($the_post, $id)
import_textpattern_post:
	array($the_post, $id)
import_movabletype_post:
	array($the_post, $id)
below_post_fields:

new_post_options:
	$sub
new_page_options:

admin_settings_nav:

admin_manage_nav:

admin_manage_users_column_header:

admin_manage_users_column:
	$user
admin_new_user_form:

admin_manage_posts_column_header:

admin_manage_posts_column:
	$post->id
admin_manage_nav:

edit_page_options:
	$_GET['id']
admin_edit_user_form:
	$edit_user
admin_manage_nav:

user_delete_list:

admin_javascript:

admin_head:

admin_nav:

